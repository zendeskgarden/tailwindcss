{
  "private": true,
  "scripts": {
    "tag": "[ `git rev-parse --abbrev-ref HEAD` = 'main' ] && standard-version --no-verify"
  },
  "workspaces": {
    "packages": [
      "example",
      "plugin"
    ],
    "nohoist": [
      "**/babel**",
      "**/eslint**",
      "**/jest**",
      "**/react-scripts**"
    ]
  },
  "devDependencies": {
    "husky": "4.2.5",
    "standard-version": "8.0.2"
  }
}
